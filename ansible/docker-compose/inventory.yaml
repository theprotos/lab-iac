all:
  hosts:
    node-ubuntu:
  children:
    unix:
      hosts:
        node-ubuntu:
          ansible-host: node-ubuntu
          ansible_user: ansible
        node-redhat:
        node-alpine:
        node-rockylinux:
      vars:
        ansible_user: ansible
        ansible_ssh_private_key_file: '~/.ssh/ansible'
